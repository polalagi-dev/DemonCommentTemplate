{
	"geode": "4.10.0",
	"gd": {
		"win": "2.2074",
		"android": "2.2074",
		"mac": "2.2074",
		"ios": "2.2074"
	},
	"id": "polalagi.demoncommenttemplate",
	"name": "Comment Templates",
	"version": "v1.0.3",
	"developer": "polalagi",
	"description": "Adds a comment template button on the comment menu for demon, weekly and daily levels.",
	"dependencies": {
		"geode.node-ids": ">=v1.20.0"
	},
	"resources": {
		"sprites": [
			"resources/*.png"
		]
	},
	"settings": {
		"template_demon": {
			"type": "string",
			"name": "Demon Comment Template",
			"match": "^.+",
			"description": "Sets the comment template for demon levels.\n<cg>AttemptCount</c> - Replaces with the amount of attempts on the level\n<cg>DemonCount</c> - Replaces with the current amount of demons beaten.\n<cg>CoinCount</c> - Replaces with the amount of completed coins.",
			"default": "AttemptCount Attempts, Demon #DemonCount, CoinCount Coins"
		},
		"template_weekly": {
			"type": "string",
			"name": "Weekly Comment Template",
			"match": "^.+",
			"description": "Sets the comment template for weekly levels.\n<cg>AttemptCount</c> - Replaces with the amount of attempts on the level\n<cg>DemonCount</c> - Replaces with the current amount of demons beaten.\n<cg>WeeklyID</c> - Replaces with the current amount of weekly levels beaten.\n<cg>CoinCount</c> - Replaces with the amount of completed coins.",
			"default": "AttemptCount Attempts, Weekly #WeeklyID, Demon #DemonCount, CoinCount Coins"
		},
		"template_daily": {
			"type": "string",
			"name": "Daily Comment Template",
			"match": "^.+",
			"description": "Sets the comment template for daily levels.\n<cg>AttemptCount</c> - Replaces with the amount of attempts on the level\n<cg>DailyID</c> - Replaces with the current amount of daily levels beaten.\n<cg>CoinCount</c> - Replaces with the amount of completed coins.",
			"default": "AttemptCount Attempts, Daily #DailyID, CoinCount Coins"
		},
		"use_old_ui": {
			"type": "bool",
			"name": "Use old UI",
			"description": "If enabled, uses the old UI.",
			"default":  false
		}
	},
	"tags": ["enhancement"]
}
