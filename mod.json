{
	"geode": "4.10.0",
	"gd": {
		"win": "2.2074",
		"android": "2.2074",
		"mac": "2.2074",
		"ios": "2.2074"
	},
	"id": "polalagi.demoncommenttemplate",
	"name": "Demon, Weekly, and Daily Comment Templates",
	"version": "v1.0.0",
	"developer": "polalagi",
	"description": "Adds a comment template button on the comment menu for demon, weekly and daily levels.",
	"dependencies": {
		"geode.node-ids": ">=v1.20.0"
	},
	"settings": {
		"template_demon": {
			"type": "string",
			"name": "Demon Comment Template",
			"description": "Sets the comment template for demon levels.\n<cg>{attempts}</c> - Replaces with the amount of attempts on the level\n<cg>{demons}</c> - Replaces with the current amount of demons beaten.",
			"default": "{attempts} Attempt(s), Demon #{demons}"
		},
		"template_weekly": {
			"type": "string",
			"name": "Weekly Comment Template",
			"description": "Sets the comment template for weekly levels.\n<cg>{attempts}</c> - Replaces with the amount of attempts on the level\n<cg>{demons}</c> - Replaces with the current amount of demons beaten.\n<cg>{weekly}</c> - Replaces with the current amount of weekly levels beaten.",
			"default": "{attempts} Attempt(s), Weekly #{weekly}, Demon #{demons}"
		},
		"template_daily": {
			"type": "string",
			"name": "Daily Comment Template",
			"description": "Sets the comment template for daily levels.\n<cg>{attempts}</c> - Replaces with the amount of attempts on the level\n<cg>{daily}</c> - Replaces with the current amount of daily levels beaten.",
			"default": "{attempts} Attempt(s), Daily #{daily}"
		}
	}
}
